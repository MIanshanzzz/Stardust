# OpenClaw 进程监控和 Claude Code Skill 部署指南

## 📋 目录
1. [进程监控部署](#进程监控部署)
2. [Claude Code Skill 使用](#claude-code-skill-使用)
3. [故障排查](#故障排查)

---

## 🔄 进程监控部署

### 方式 1：手动启动（推荐用于测试）

```powershell
cd C:\Users\Administrator\.openclaw\workspace
.\openclaw-monitor.ps1
```

### 方式 2：使用后台运行

```powershell
cd C:\Users\Administrator\.openclaw\workspace
Start-Process PowerShell -ArgumentList "-NoProfile -ExecutionPolicy Bypass -File openclaw-monitor.ps1" -WindowStyle Hidden
```

### 方式 3：注册为系统服务（推荐用于长期运行）

```powershell
# 1. 以管理员身份运行 PowerShell

# 2. 创建服务
sc.exe create OpenClawMonitor `
    binPath= "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.ps1" `
    start= auto `
    DisplayName= "OpenClaw 进程监控" `
    Description= "监控 OpenClaw 进程，自动重启卡死的进程"

# 3. 启动服务
sc.exe start OpenClawMonitor

# 4. 检查服务状态
sc.exe query OpenClawMonitor
```

### 方式 4：使用 NSSM（如果安装了）

```powershell
# 下载 NSSM：https://nssm.cc/download
nssm install OpenClawMonitor "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe"
nssm set OpenClawMonitor AppParameters "-NoProfile -ExecutionPolicy Bypass -File C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.ps1"
nssm start OpenClawMonitor
```

### 服务管理命令

```powershell
# 启动服务
sc.exe start OpenClawMonitor

# 停止服务
sc.exe stop OpenClawMonitor

# 重启服务
sc.exe restart OpenClawMonitor

# 查看服务状态
sc.exe query OpenClawMonitor

# 删除服务
sc.exe delete OpenClawMonitor

# 查看服务日志
Get-Content "C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.log" -Tail 50
```

### 日志文件位置
```
C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.log
```

---

## 💻 Claude Code Skill 使用

### 1. 安装 Claude Code（如果未安装）

```powershell
# 方式 1：使用 winget
winget install Anthropic.ClaudeCode

# 方式 2：使用 PowerShell
irm https://claude.ai/install.ps1 | iex
```

### 2. 登录 Claude Code

```powershell
claude
```

首次使用需要登录，按照提示操作。

### 3. 在 OpenClaw 中使用

#### 方式 1：直接在对话中使用

```
@claudecode 用 TypeScript 重写这个 React 组件
@claudecode 为什么这个函数会返回 undefined？
@claudecode 分析我的代码库结构
```

#### 方式 2：使用 Skill

```
请使用 claude-code skill 来帮我查看这个文件
```

#### 方式 3：使用 JSON 格式

```json
{
  "skill": "claude-code",
  "command": "build",
  "prompt": "用 TypeScript 重写这个 React 组件"
}
```

### 4. 使用场景

#### 场景 1：构建功能
```
@claudecode 用 TypeScript 重写这个 React 组件
```
Claude Code 会：
1. 读取你的组件代码
2. 用 TypeScript 重写
3. 确保类型安全
4. 保持原有功能

#### 场景 2：调试问题
```
@claudecode 为什么这个函数会返回 undefined？
```
Claude Code 会：
1. 读取你的代码
2. 分析返回值
3. 找出问题所在
4. 提供修复方案

#### 场景 3：分析代码库
```
@claudecode 分析我的代码库结构
```
Claude Code 会：
1. 扫描你的代码库
2. 识别主要模块
3. 分析依赖关系
4. 提供架构建议

---

## 🔧 故障排查

### 进程监控问题

#### 问题 1：监控脚本无法启动
```powershell
# 检查 PowerShell 执行策略
Get-ExecutionPolicy

# 临时修改执行策略
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

#### 问题 2：服务启动失败
```powershell
# 查看服务日志
Get-Content "C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.log" -Tail 50

# 检查服务状态
sc.exe query OpenClawMonitor

# 手动运行脚本测试
cd C:\Users\Administrator\.openclaw\workspace
.\openclaw-monitor.ps1
```

#### 问题 3：进程重启失败
```powershell
# 检查 OpenClaw 可执行文件路径
Test-Path "C:\Users\Administrator\AppData\Roaming\npm\node_modules\openclaw\bin\openclaw.exe"

# 手动启动 OpenClaw 测试
.\openclaw.exe
```

#### 问题 4：CPU 使用率为 0 时误判为卡住
```powershell
# 编辑 openclaw-monitor.ps1
# 修改 frozenThreshold 从 1800（30分钟）改为更长时间，如 3600（1小时）
$frozenThreshold = 3600
```

### Claude Code 问题

#### 问题 1：Claude Code 未安装
```powershell
# 检查是否安装
claude --version

# 如果未安装，运行：
winget install Anthropic.ClaudeCode
```

#### 问题 2：登录失败
```powershell
# 清除缓存
claude logout
claude login
```

#### 问题 3：权限不足
```powershell
# 确保当前用户有读写权限
Test-Path .
Get-ChildItem .
```

#### 问题 4：Skill 不生效
```powershell
# 重新加载 Skill
# 在 OpenClaw 中运行：
/openclaw reload-skills

# 或者重启 OpenClaw
# 然后检查 Skill 是否注册成功
/openclaw list-skills
```

---

## 📊 监控指标

### 进程监控指标
- 进程运行时间
- CPU 使用率
- 内存使用量
- 检查间隔（5分钟）

### 重启记录
- 重启时间
- 重启原因
- 重启次数

---

## 🚀 快速启动脚本

创建一个快捷启动脚本 `start-monitor.bat`：

```batch
@echo off
cd /d C:\Users\Administrator\.openclaw\workspace
start /B powershell.exe -NoProfile -ExecutionPolicy Bypass -File openclaw-monitor.ps1
echo OpenClaw 进程监控已启动
echo 日志文件: openclaw-monitor.log
pause
```

双击运行即可启动监控！

---

## 📝 维护建议

1. **定期检查日志**
   ```powershell
   Get-Content "C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.log" -Tail 100
   ```

2. **定期重启服务**
   ```powershell
   sc.exe restart OpenClawMonitor
   ```

3. **备份配置**
   ```powershell
   Copy-Item "C:\Users\Administrator\.openclaw\workspace\*.ps1" "C:\Users\Administrator\.openclaw\workspace\backup\"
   ```

4. **更新监控脚本**
   - 定期检查并更新监控脚本
   - 根据实际情况调整检查阈值

---

**最后更新**: 2026-02-03
**维护者**: 老大
**版本**: 1.0

# 📋 Obsidian 模板使用指南

## 已创建的模板

我已经为你创建了**3个模板文件**：

### 1. 通用模板
**位置**: `.obsidian/templates/template.md`
**用途**: 创建文章、笔记、文档

### 2. 任务模板
**位置**: `.obsidian/templates/task-template.md`
**用途**: 创建待办事项、开发任务

### 3. 日志模板
**位置**: `.obsidian/templates/daily-log.md`
**用途**: 每日开发日志

## 如何使用模板

### 方法1：使用快捷键（推荐）

1. **安装 Templater 插件**
   - 打开 Obsidian → 设置 → 社区插件
   - 搜索 "Templater" → 安装 → 启用

2. **设置模板路径**
   - 设置 → 外观 → 标签栏
   - 添加路径: `C:\Users\Administrator\AppData\Roaming\Obsidian\Vault\.obsidian\templates`

3. **使用模板**
   - 按 `Ctrl + ;` 或 `Ctrl + P` → 输入 "Templater"
   - 选择模板（如 `daily-log`）
   - 输入变量（如日期、标题）

### 方法2：手动创建

1. 按 `Ctrl + P`
2. 输入 "创建新笔记"
3. 点击 `.obsidian/templates` 目录中的模板
4. 修改变量（日期、标题等）

## 变量说明

所有模板都支持以下变量：

```yaml
<%= date.now('YYYY-MM-DD') %>           # 当前日期
<%= date.format('YYYY年MM月DD日') %>     # 中文日期格式
<%= date.now('YYYY-MM-DD HH:mm:ss') %>   # 完整时间戳
```

## 快速测试

1. **创建一个任务**
   ```
   Ctrl + ; → 选择 task-template.md
   在标题处输入 "测试任务"
   保存
   ```

2. **创建每日日志**
   ```
   Ctrl + ; → 选择 daily-log.md
   系统自动填入今天的日期
   保存
   ```

## 更多模板资源

- 官方模板库: https://forum.obsidian.md/c/shared-templates
- Obsidian模板API: https://help.obsidian.md/Editing+and+formatting/Template+syntax

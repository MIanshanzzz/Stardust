# OpenClaw 自动优化方案

## ✅ 已创建的文件

### 监控相关
1. ✅ `openclaw-monitor.ps1` - 主监控脚本（每5分钟检查一次）
2. ✅ `restart-openclaw.ps1` - 重启脚本
3. ✅ `openclaw-monitor.service` - 系统服务配置
4. ✅ `start-monitor.bat` - 快速启动脚本
5. ✅ `部署指南.md` - 完整部署文档

### Claude Code Skill
6. ✅ `skills/claude-code/SKILL.md` - Skill 说明文档
7. ✅ `skills/claude-code/skill.json` - Skill 配置文件

---

## 🚀 快速启动（3种方式）

### 方式 1：双击启动（最简单）
```
双击 start-monitor.bat
```

### 方式 2：手动启动
```powershell
cd C:\Users\Administrator\.openclaw\workspace
.\openclaw-monitor.ps1
```

### 方式 3：注册为系统服务（推荐）
```powershell
# 以管理员身份运行 PowerShell
sc.exe create OpenClawMonitor `
    binPath= "C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\Users\Administrator\.openclaw\workspace\openclaw-monitor.ps1" `
    start= auto `
    DisplayName= "OpenClaw 进程监控" `
    Description= "监控 OpenClaw 进程，自动重启卡死的进程"

sc.exe start OpenClawMonitor
```

---

## 🔍 监控原理

1. **每5分钟检查一次**
   - 检查 OpenClaw 进程是否存在
   - 检查进程运行时间（超过30分钟未响应会重启）
   - 检查 CPU 使用率（长时间为0会重启）

2. **自动重启**
   - 如果检测到卡住，自动停止 OpenClaw 进程
   - 2秒后重新启动 OpenClaw

3. **日志记录**
   - 所有操作都会记录到 `openclaw-monitor.log`
   - 可以随时查看监控状态

---

## 💻 Claude Code Skill 使用

### 安装 Claude Code
```powershell
winget install Anthropic.ClaudeCode
claude  # 首次登录
```

### 使用方法
```
@claudecode 用 TypeScript 重写这个组件
@claudecode 为什么这个函数会报错？
@claudecode 分析我的代码库
```

### 支持的命令
- `build` - 构建功能
- `debug` - 调试问题
- `navigate` - 导航代码库
- `automate` - 自动化任务
- `analyze` - 分析代码库

---

## 📊 查看监控日志

```powershell
# 查看最近50行
Get-Content "openclaw-monitor.log" -Tail 50

# 查看全部日志
Get-Content "openclaw-monitor.log"

# 实时监控
Get-Content "openclaw-monitor.log" -Wait -Tail 50
```

---

## 🛠️ 故障排查

### 监控脚本无法启动
```powershell
# 临时修改执行策略
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

### 服务启动失败
```powershell
# 查看服务状态
sc.exe query OpenClawMonitor

# 手动测试脚本
cd C:\Users\Administrator\.openclaw\workspace
.\openclaw-monitor.ps1
```

### OpenClaw 无法启动
```powershell
# 检查可执行文件
Test-Path "C:\Users\Administrator\AppData\Roaming\npm\node_modules\openclaw\bin\openclaw.exe"

# 手动启动测试
.\openclaw.exe
```

---

## 📝 注意事项

1. **执行策略**
   - PowerShell 默认执行策略可能阻止脚本运行
   - 使用 `-ExecutionPolicy Bypass` 跳过检查

2. **后台运行**
   - 监控脚本会在后台运行
   - 关闭此窗口不会停止监控

3. **重启频率**
   - 默认30分钟未响应会重启
   - 可以修改 `openclaw-monitor.ps1` 中的 `$frozenThreshold` 调整

4. **Skill 加载**
   - Claude Code Skill 可能需要重启 OpenClaw 才能生效
   - 运行 `/openclaw reload-skills` 重新加载

---

## 🎯 推荐方案

**对于长期稳定运行**：注册为系统服务（方式 3）

**对于测试和开发**：双击启动（方式 1）

**对于临时使用**：手动启动（方式 2）

---

**创建时间**: 2026-02-03
**版本**: 1.0
**维护**: 老大
